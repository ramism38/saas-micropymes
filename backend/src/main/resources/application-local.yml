spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/micropymes
    username: micropymes
    password: micropymes

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    session:
      cookie:
        http-only: true
        secure: false
        same-site: lax